#include main.h

/**
 * char *_stract - concatenates two strings
 * @*dest: first string
 * @*src: second string
 */

char *_strcat(char *dest, char *src)
{
  char dest[];
  char src[];

  strcat(dest, src);

  return (0);
}
